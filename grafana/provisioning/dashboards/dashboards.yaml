apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: ''
    type: 'file'
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10  # Interval to check for updates (optional)
    options:
      path: 'dash.json'
